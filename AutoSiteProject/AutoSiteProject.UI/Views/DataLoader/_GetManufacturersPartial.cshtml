@using System.Web.UI.WebControls
@model IEnumerable<AutoSiteProject.Models.ViewModels.ManufacturerViewModel>

<h2>List countries</h2>
@Html.DevExpress().GridView(settings =>
{
    settings.Name = "manufacturersGridView";
    settings.CallbackRouteValues = new { Controller = "DataLoader", Action = "GetManufacturersPartial" };
    settings.Width = Unit.Percentage(100);
    settings.Columns.Add(column =>
    {
        column.Caption = "#";
        column.SetDataItemTemplateContent(c =>
        {
            ViewContext.Writer.Write(
                Html.ActionLink("Edit", "Edit", "Manufacturer", routeValues: new { Id = DataBinder.Eval(c.DataItem, "Id") }, htmlAttributes: new { })
                + "&nbsp;" +
                Html.ActionLink("Delete", "Delete", "Manufacturer", routeValues: new { Id = DataBinder.Eval(c.DataItem, "Id") }, htmlAttributes: new { })
                );
        });
        column.SetHeaderTemplateContent(c =>
        {
            ViewContext.Writer.Write(
                Html.ActionLink("New", "Create", "Manufacturer").ToHtmlString()
                );
        });
        column.Settings.AllowDragDrop = DefaultBoolean.False;
        column.Settings.AllowSort = DefaultBoolean.False;
        column.Width = 70;
    });
    settings.Columns.Add("Id");
    settings.Columns.Add("Name");
    settings.Columns.Add(column =>
    {
        column.Caption = "Country";
        column.SetDataItemTemplateContent(c =>
        {
            ViewContext.Writer.Write(
                DataBinder.Eval(c.DataItem, "Country.Name")
                );
        });
    });
    settings.KeyFieldName = "Id";
}).Bind(Model).GetHtml()






